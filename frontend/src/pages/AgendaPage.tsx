import { CalendarDays } from 'lucide-react';
import { PlaceholderPage } from '@/components/PlaceholderPage';

export function AgendaPage() {
  return (
    <PlaceholderPage
      icon={CalendarDays}
      title="Agenda"
      description="Visualizações por dia, semana e mês da agenda da equipe serão implementadas na próxima etapa."
    />
  );
}
